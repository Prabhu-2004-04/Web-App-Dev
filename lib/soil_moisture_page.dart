import 'package:flutter/material.dart';

class SoilMoisturePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Soil Moisture Monitoring', style: TextStyle(fontSize: 24)),
    );
  }
}
